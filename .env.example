# ===================================
# Local Development Configuration
# ===================================

# Database Configuration (Local PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_local_postgres_password
DB_NAME=task_manager_db

# Flask Configuration
SECRET_KEY=your-secret-key-here-change-this-in-production
FLASK_ENV=development

# Optional: Port for local development
PORT=5000

# Database Retry Configuration
DB_RETRY_SECONDS=2
DB_RETRY_MAX=10

# ===================================
# Production Configuration (Render + NeonDB)
# ===================================
# These are set automatically by Render, but you can override them

# DATABASE_URL will be automatically provided by Render/NeonDB
# Format: postgresql://user:password@host:port/database?sslmode=require
# DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require

# Flask Configuration for Production
# FLASK_ENV=production
# SECRET_KEY=use-render-secret-generator-or-strong-random-key

# Production Port (set by Render)
# PORT=10000

# ===================================
# NeonDB Specific Configuration
# ===================================
# If using NeonDB external database:
# 1. Go to https://neon.tech
# 2. Create a new project
# 3. Copy the connection string
# 4. Set it as DATABASE_URL in Render environment variables